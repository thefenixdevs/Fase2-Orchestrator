apiVersion: v1
kind: Service
metadata:
  name: users-api-service
  namespace: fiap-gamestore
  labels:
    app: users-api
    component: api
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: users-api
