apiVersion: v1
kind: Service
metadata:
  name: postgres-users-service
  namespace: fiap-gamestore
  labels:
    app: postgres-users
    component: database
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: postgres-users
