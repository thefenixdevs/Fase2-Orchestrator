apiVersion: v1
kind: Secret
metadata:
  name: shared-secrets
  namespace: fiap-gamestore
  labels:
    app: fiap-gamestore
type: Opaque
stringData:
  # Credenciais do RabbitMQ (compartilhado)
  rabbitmq-username: "guest"
  rabbitmq-password: "guest"

---
# NOTA: Em produção, use ferramentas como:
# - Sealed Secrets (https://github.com/bitnami-labs/sealed-secrets)
# - External Secrets Operator (https://external-secrets.io/)
# - Azure Key Vault / AWS Secrets Manager / GCP Secret Manager
# - HashiCorp Vault
#
# Para criar este secret manualmente:
# kubectl create secret generic shared-secrets \
#   --from-literal=rabbitmq-username="guest" \
#   --from-literal=rabbitmq-password="guest" \
#   --namespace=fiap-gamestore
