apiVersion: v1
kind: Service
metadata:
  name: gateway-api-service
  namespace: fiap-gamestore
  labels:
    app: gateway-api
    component: gateway
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: gateway-api
