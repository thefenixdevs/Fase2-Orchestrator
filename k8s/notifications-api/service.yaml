apiVersion: v1
kind: Service
metadata:
  name: notifications-api-service
  namespace: fiap-gamestore
  labels:
    app: notifications-api
    component: worker
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: notifications-api
